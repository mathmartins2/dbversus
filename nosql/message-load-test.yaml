config:
  target: "http://localhost:3000/nosql"
  phases:
    - duration: 10
      arrivalRate: 9999
  defaults:
    headers:
      Content-Type: "application/json"

scenarios:
  - flow:
      - post:
          url: "/message"
          json:
            content: "Test message"
